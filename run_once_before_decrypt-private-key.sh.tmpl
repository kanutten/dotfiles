#!/bin/sh

if [ ! -f "/var/home/magnar/.config/chezmoi/key.txt" ]; then
    mkdir -p "/var/home/magnar/.config/chezmoi"
    chezmoi age decrypt --output "/var/home/magnar/.config/chezmoi/key.txt" --passphrase "{{ .chezmoi.sourceDir }}/key.txt.age"
    chmod 600 "/var/home/magnar/.config/chezmoi/key.txt"
fi
