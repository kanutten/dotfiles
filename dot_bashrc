# .bashrc

# Source global definitions
if [ -f /etc/bashrc ]; then
	. /etc/bashrc
fi

# User specific environment
if ! [[ "$PATH" =~ "$HOME/.local/bin:$HOME/bin:" ]]
then
    PATH="$HOME/.local/bin:$HOME/bin:$PATH"
fi
export PATH

# Uncomment the following line if you don't like systemctl's auto-paging feature:
# export SYSTEMD_PAGER=

# Set the default editor
if command -v micro &> /dev/null; then
    export EDITOR=micro
    export VISUAL=micro
    alias vim='micro'
    alias vi='micro'
    alias svi='sudo micro'
    alias nano='micro'
    alias vis='micro "+set si"'
else
    export EDITOR=micro
    export VISUAL=micro
fi
alias spico='sudo pico'
alias snano='sudo nano'

# User specific aliases and functions
if [ -d ~/.bashrc.d ]; then
	for rc in ~/.bashrc.d/*; do
		if [ -f "$rc" ]; then
			. "$rc"
		fi
	done
fi

unset rc

[[ -f /usr/share/bash-prexec ]] && source /usr/share/bash-prexec
[[ "$(command -v atuin)" ]] && eval "$(atuin init bash)"
alias lk='eza --all --long --group --group-directories-first --icons --header --time-style long-iso'
alias lf='eza -lF --color=always | grep -v /'
alias lh='eza -dl .* --group-directories-first'
#alias ll='eza -al --group-directories-first'
#alias lk='eza -alF --color=always --sort=size | grep -v /'
alias lt='eza -al --sort=modified'
alias mi='micro'
alias lg='lazygit'
alias mc='mc --nosubshell'
alias ch='chezmoi'
alias cha='chezmoi -v apply'
alias chd='chezmoi diff'
alias chm='chezmoi managed'

### bling.sh source start
test -f /usr/share/ublue-os/bling/bling.sh && source /usr/share/ublue-os/bling/bling.sh
### bling.sh source end
